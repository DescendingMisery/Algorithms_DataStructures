#include <iostream>
/*
int main()
{
	int a = 10;
	int& r = a;
	int b = 30;
	r = b; // because r references a, so when this assignment happens, r changes value, so will a 

	printf("%i", r);
	return 0;
}
*/