#include<iostream>
#include<stdio.h>

using namespace std;

/*int main()
{
	int A[5] = { 1,3,4,5,6 };


	cout << sizeof(A) << endl; //sizeof() returns the size of a type in bytes, so an int is 4 bytes, the array contains 5 elements so 4 x 5 = 20.
	cout << A[4] << endl;
	printf("%d\n", A[2]);

	for (int i = 0; i < 5; i++)
	{
		cout << A[i] << endl;
	}
	return 0;
}*/